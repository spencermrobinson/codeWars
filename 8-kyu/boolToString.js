//Write a function the receives a boolean as a parameter.  If the boolean is true it returns the string "true" if the boolean is false it returns the string "false".

function booleanToString(b){
    if(b === true){
    return "true"
    }else{
    return "false"
    }
  }